[build-system]
# No setuptools-scm: the version is written in this file and cut by hand at release, so a
# build has nothing to read out of git and was downloading a package it never used.
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "netbox-change-control"
version = "0.5.0"
description = "Policy-driven change control and mandatory review for NetBox branches"
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
authors = [{ name = "Antoine Keranflech", email = "antoine.keranflech@gmail.com" }]
keywords = ["netbox", "netbox-plugin", "change-management", "change-control", "branching", "approval-workflow", "network-automation"]
# netbox-branching 1.2 is the floor because NetBox 4.7 is: 1.1.x still uses django-mptt,
# whose columns 4.7 drops, and 1.2 declares NetBox 4.7 as its own floor. The two lines do not
# overlap, so this release supports 4.7 only; the 0.4.x line stays on NetBox 4.6 with 1.1.x.
# The pre-release is named explicitly because 1.2.0 is not out yet, and pip skips a
# pre-release unless the specifier asks for one. The final 1.2.0 satisfies this too.
dependencies = ["netboxlabs-netbox-branching>=1.2.0b1,<1.3"]
classifiers = [
  # Below 1.0: the models, the settings and the REST API can still change, which the README
  # says as well. This is what says it on the PyPI page.
  "Development Status :: 4 - Beta",
  "Framework :: Django",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Intended Audience :: System Administrators",
]

[project.urls]
Homepage = "https://github.com/antoinekh/netbox-change-control"
Documentation = "https://antoinekh.github.io/netbox-change-control/"
Source = "https://github.com/antoinekh/netbox-change-control"
Changelog = "https://antoinekh.github.io/netbox-change-control/changelog/"
Issues = "https://github.com/antoinekh/netbox-change-control/issues"

[tool.setuptools.packages.find]
# The test suite ships with the package, as netbox-branching's does, so it can be run
# against an installed copy. The seed commands do not: they live in dev/ as a separate
# development-only plugin, so a production install has no way to create demo data.
include = ["netbox_change_control*"]

[tool.setuptools.package-data]
netbox_change_control = ["templates/**/*.html"]

[tool.ruff]
line-length = 120
# Matches requires-python, so the UP rules can use everything the project can.
target-version = "py312"
# Django writes these; they are not hand-maintained source.
extend-exclude = ["*/migrations/*"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "RUF"]

[tool.ruff.format]
quote-style = "single"
